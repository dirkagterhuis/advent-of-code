var fs = require('fs');

var input = [];
fs.readFileSync('./input.txt', 'utf-8')
    .trim()
    .replace(/\r\n/g, '\n')
    .split(/\n/g)
    .forEach(element => {
        var split = element.split(' ');
        input.push({ 

        });
    })
